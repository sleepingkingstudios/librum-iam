# frozen_string_literal: true

module Librum::Iam::Authentication::Sessions::Commands
  # Clears the current authentication session.
  class Destroy < Cuprum::Rails::Command
    private

    def process(**) = nil
  end
end
